# GOVERNANCE.yaml — project governance manifest for quickcasepro
# Generated by `ehgov init` (governance ehudso7/project-governance v1.1.0).
# This file is project-owned after generation: edit deliberately, keep it valid
# (`ehgov validate`), and bump `governance.version` only via `ehgov sync`.
governance:
  source: ehudso7/project-governance
  version: 1.2.0
  schema_version: 1
  profile: web-saas
project:
  id: quickcasepro
  name: quickcasepro
  type: web-saas
  criticality: medium
  lifecycle: active
agents:
  claude_code: true
  codex: false
  human_approval_required_for:
    - production-deployment
    - destructive-migration
    - secrets
    - billing
    - authentication
    - authorization
graphs:
  default:
    feature: feature-development
    bug: bug-resolution
    architecture: architecture-review
    release: production-release
    incident: incident-response
    audit: repository-audit
    security: security-review
    dependency: dependency-upgrade
policies:
  required:
    - base
    - security
    - delivery
    - ai-agents
evidence:
  output_directory: .governance/evidence
  retain_days: 365
  require_execution_record: true
sensitive_paths:
  - ".github/workflows/**"
  - "**/migrations/**"
  - "infra/**"
  - "**/*auth*"
  - "**/*billing*"
  - "**/*permission*"
  - "**/*secret*"
sync:
  managed_files:
    - CLAUDE.md
    - AGENTS.md
    - PROJECT.md
    - GRAPH.md
    - EVIDENCE.md
    - DECISIONS.md
